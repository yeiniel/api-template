import { User } from './user';

describe('models/user', () => {
    describe(User.name, () => {
       it('should exist', () => {
           // given
           // when
           // then
           expect(User).toBeDefined();
       }); 
    });
})