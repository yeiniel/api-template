export enum Role {
  Admin = 1,
  Sales = 5,
  Support = 7,
  Owner = 10,
  Guest = 100,
}

export enum RolePretty {
  Admin = 'Admin',
  Support = 'Technical Support',
  Sales = 'Sales',
  Owner = 'Owner',
  Guest = 'Guest',
}
