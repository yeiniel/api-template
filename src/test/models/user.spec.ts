import { User } from '../../models/user';

describe('models/user', () => {
    describe('User', () => {
        it('should exist', () => {
            expect(User).toBeDefined();
        })
    });
});